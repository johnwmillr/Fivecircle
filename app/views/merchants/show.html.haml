-# in app/views/merchants/show.html.haml

%h1 Merchant #{@current_merchant.merchantName} Profile
-# TODO: Actually, don't we just want a static map for Merchants?
%p
.card
  .card-block
    #map

%h1 Merchant Coupons

%table{:border => 1}
  #coupons
    %thead
      %th Merchant ID
      %th Name
      %th Date
      %th Location
      %th View Coupon
    %tbody
      - @coupons.each do |coupon|
        - if coupon.merchant_id == @merchant.id
          %tr     
            %td= @merchant.id   
            %td= coupon.couponName
            %td= coupon.created_at
            %td= @merchant.address
            %td= link_to "View coupon", merchant_coupon_path(@merchant.id,coupon.id)

- if @coupons.length == 0
  You haven't made any coupons yet.
  %div
  
= link_to "Add new coupon", new_merchant_coupon_path(@merchant.id)